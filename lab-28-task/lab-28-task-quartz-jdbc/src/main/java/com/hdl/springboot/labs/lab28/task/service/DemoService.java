package com.hdl.springboot.labs.lab28.task.service;

import org.springframework.stereotype.Service;

/**
 * Bean Spring rỗng — minh họa {@code @Autowired} vào {@link QuartzJobBean}.
 *
 * <p>Lab không query DB {@code user}; datasource user chỉ mô phỏng kiến trúc prod.
 */
@Service
public class DemoService {
}